version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - ls -l
            - amplify pull --appId $APP_ID --envName $AWS_BRANCH --y
            - yarn install
        build:
          commands:
            - yarn run build
            - cat src/aws-exports.js
      artifacts:
        baseDirectory: build
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: admin
